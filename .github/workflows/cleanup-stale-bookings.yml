name: Cleanup stale booking slots

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Trigger stale booking cleanup
        run: >
          curl --fail --silent --show-error
          --header "User-Agent: boat4two-cleanup-bot"
          https://boat4two.com/api/cleanup-holds
